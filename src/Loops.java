
public class Loops {

	public static void main(String[] args) {
		//Loops allow performing action over and over again until something happens e.g. scoop 4 cups of flour into bowl for baking. Loops will scoop flour into cup until right amount met for cup.
        //Types of loops: forloops(for)- if you have a specific no. of iterations you want to perform for something or have a group of items. 
		//(variable iteration = x; boolean expression condition; post iteration/increment)
		for (int i = 0; i < 10; i++) { //(variable iteration = x; boolean expression condition; post iteration/increment); 
		}
	}
}
